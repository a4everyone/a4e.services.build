FROM alpine:3.12

RUN true \
    && apk add --no-cache openssh-sftp-server dropbear dropbear-convert rsync acl iptables \
    && rm -rf /var/cache/apk/* \
    && mkdir /etc/dropbear \
    && touch /var/log/lastlog
